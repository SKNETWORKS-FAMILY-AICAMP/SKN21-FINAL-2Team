# 테스트 계획 및 결과 보고서

## 목차
1. [테스트 목적](#1-테스트-목적)
2. [테스트 환경](#2-테스트-환경)
   - 2.1 하드웨어 환경
   - 2.2 소프트웨어 환경
3. [테스트 케이스](#3-테스트-케이스)
   - 3.1 단위 테스트 (Unit Test)
   - 3.2 API 통합 테스트 (Integration Test)
   - 3.3 페르소나 기반 답변 생성 테스트 (Persona Test)
   - 3.4 예외 처리 테스트 (Exception Test)
   - 3.5 성능 테스트 (Performance Test)
4. [RAG 파이프라인 모델링 평가 (3-Stage)](#4-rag-파이프라인-모델링-평가-3-stage)
   - 4.1 Stage 1: 검색(Retrieval) 평가
   - 4.2 Stage 2: 추천(Recommendation) 평가
   - 4.3 Stage 3: 생성(Generation) 평가
5. [종합 결과 및 개선 사항](#5-종합-결과-및-개선-사항)
   - 5.1 테스트 통과율 요약
   - 5.2 발견된 이슈 및 개선 사항
   - 5.3 결론

---

# 1. 테스트 목적

기능적 및 비기능적 요구 사항을 충족하는지 확인하고, RAG 파이프라인의 검색 품질·답변 정확도·시스템 안정성을 종합적으로 검증한다.

---

# 2. 테스트 환경

## 2.1 하드웨어 환경
| 구분 | 사양 |
|------|------|
| 개발/테스트 머신 | (작성 필요) |
| GPU | (작성 필요 — CUDA / Apple MPS / CPU) |
| RAM | (작성 필요) |

## 2.2 소프트웨어 환경
| 구분 | 버전/상세 |
|------|----------|
| OS | macOS |
| Python | 3.11+ |
| Backend Framework | FastAPI + Uvicorn |
| Frontend Framework | Next.js |
| LLM | OpenAI gpt-4o-mini |
| Embedding (Text) | BAAI/bge-m3 (1024-dim) |
| Embedding (Image) | CLIP-ViT-L-14 (768-dim) |
| Reranker | BAAI/bge-reranker-base (CrossEncoder) |
| Vector DB | Qdrant (places / photos 컬렉션) |
| RDB | MySQL 8.0 |
| 컨테이너 | Docker Compose |
| 테스트 프레임워크 | pytest |
| RAG 평가 도구 | RAGAS (Faithfulness, AnswerRelevancy 등) |

---

# 3. 테스트 케이스

## 3.1 단위 테스트 (Unit Test)

시스템의 개별 모듈이 독립적으로 정확하게 동작하는지 검증한다.

### 3.1.1 LangGraph 라우팅 테스트
| TC ID | 테스트 항목 | 검증 내용 | 결과 |
|-------|-----------|----------|------|
| UT-01 | Intent → Planner 라우팅 | `primary_intent=TRIP_PLANNING`일 때 planner 노드로 분기되는지 | |
| UT-02 | Intent → Retriever 라우팅 | `PLACE_INQUIRY` 등 일반 검색 의도 시 retriever로 분기되는지 | |
| UT-03 | Intent → General 라우팅 | `GENERAL` 의도 시 executor_general로 분기되는지 | |
| UT-04 | Missing Slot → Executor Missing 라우팅 | `missing_slots`가 존재할 때 executor_missing으로 분기되는지 | |

### 3.1.2 검색 모듈 테스트
| TC ID | 테스트 항목 | 검증 내용 | 결과 |
|-------|-----------|----------|------|
| UT-05 | Place ID 추출 우선순위 | photos 컬렉션에서 payload의 contentid를 우선 사용하는지 | |
| UT-06 | 카테고리 정규화 | "맛집", "식당" → "음식점"으로 정규화되는지 | |
| UT-07 | 검색 프로필 설정 | serving/evaluation 프로필별 파라미터(candidate_k, top_k)가 올바른지 | |
| UT-08 | 미지정 프로필 폴백 | 알 수 없는 프로필 입력 시 serving으로 폴백되는지 | |
| UT-09 | 리랭커 폴백 | 리랭커 실패 시 기존 점수 기반으로 정상 반환되는지 | |

### 3.1.3 스트리밍 버퍼링 테스트
| TC ID | 테스트 항목 | 검증 내용 | 결과 |
|-------|-----------|----------|------|
| UT-10 | 마크다운 링크 버퍼링 | 미완성 `[text](url...` 상태에서 클라이언트에 전송하지 않는지 | |
| UT-11 | IDs 태그 제거 | `[IDs: 123, 456]` 태그가 사용자에게 노출되지 않는지 | |
| UT-12 | Raw URL 버퍼링 | 미완성 URL이 끊기지 않고 완성 후 전송되는지 | |

### 3.1.4 Executor 검증 테스트
| TC ID | 테스트 항목 | 검증 내용 | 결과 |
|-------|-----------|----------|------|
| UT-13 | selected_ids 유효성 검증 | 존재하지 않는 place ID가 필터링되는지 | |
| UT-14 | 토큰 스트림 디스패치 | executor가 토큰을 정상적으로 SSE로 전달하는지 | |

---

## 3.2 API 통합 테스트 (Integration Test)

API 엔드포인트가 기대하는 요청/응답 흐름으로 동작하는지 검증한다.

### 3.2.1 인증 API
| TC ID | 테스트 항목 | 검증 내용 | 결과 |
|-------|-----------|----------|------|
| IT-01 | Google 로그인 성공 | 유효한 토큰으로 로그인 시 JWT 발급되는지 | |
| IT-02 | Google 로그인 실패 | 무효한 토큰으로 로그인 시 적절한 에러 코드 반환 | |
| IT-03 | Refresh Token 갱신 | 만료된 access token을 refresh token으로 갱신 가능한지 | |

### 3.2.2 채팅 API
| TC ID | 테스트 항목 | 검증 내용 | 결과 |
|-------|-----------|----------|------|
| IT-04 | 채팅방 생성 | POST `/api/chat/rooms` 정상 동작 | |
| IT-05 | 채팅방 목록 조회 | GET `/api/chat/rooms` 사용자별 필터링 | |
| IT-06 | 메시지 전송 (비스트리밍) | POST `/api/chat/rooms/{room_id}/ask` 정상 응답 | |
| IT-07 | SSE 스트리밍 응답 | POST `/api/chat/ask` SSE 이벤트 형식 검증 | |
| IT-08 | 자동 제목 생성 | 첫 2회 메시지 내에 채팅방 제목이 자동 설정되는지 | |
| IT-09 | 북마크 기능 | 채팅방/장소 북마크 CRUD | |

---

## 3.3 페르소나 기반 답변 생성 테스트 (Persona Test)

8개의 사용자 페르소나를 설정하고, 페르소나 당 5개 질문(총 40개)을 통해 답변 품질을 평가한다.

### 3.3.1 페르소나 목록
| # | 페르소나 | 특징 | 예상 질문 유형 |
|---|---------|------|--------------|
| 1 | (작성 필요) | | |
| 2 | (작성 필요) | | |
| 3 | (작성 필요) | | |
| 4 | (작성 필요) | | |
| 5 | (작성 필요) | | |
| 6 | (작성 필요) | | |
| 7 | (작성 필요) | | |
| 8 | (작성 필요) | | |

### 3.3.2 평가 기준
| 평가 항목 | 설명 | 배점 |
|----------|------|------|
| 관련성 | 질문 의도에 맞는 답변인지 | /5 |
| 정확성 | 추천 장소/정보가 실제로 존재하고 정확한지 | /5 |
| 완성도 | 충분한 정보(주소, 특징, 링크 등)를 포함하는지 | /5 |
| 자연스러움 | 대화체가 자연스럽고 친근한지 | /5 |
| 개인화 반영 | 사용자 선호도/맥락이 답변에 반영되었는지 | /5 |

### 3.3.3 페르소나별 테스트 결과
(페르소나별 5개 질문 × 답변 × 채점 결과 표 — 추후 작성)

---

## 3.4 예외 처리 테스트 (Exception Test)

비정상 입력 및 장애 상황에서 시스템이 안전하게 동작하는지 검증한다.

| TC ID | 테스트 항목 | 시나리오 | 기대 결과 | 결과 |
|-------|-----------|---------|----------|------|
| EX-01 | 빈 입력 처리 | 빈 문자열 또는 공백만 전송 | 적절한 안내 메시지 반환 | |
| EX-02 | 초장문 입력 | 매우 긴 텍스트 입력 | 정상 처리 또는 길이 제한 안내 | |
| EX-03 | 검색 결과 없음 | 존재하지 않는 장소 질문 | Tavily 웹 검색 폴백 동작 | |
| EX-04 | LLM API 장애 | OpenAI API 타임아웃/오류 | 기본 에러 메시지 반환, 서버 미중단 | |
| EX-05 | Vector DB 연결 실패 | Qdrant 접속 불가 | 에러 로그 + 사용자 안내 | |
| EX-06 | 잘못된 이미지 URL | 유효하지 않은 image_path 전송 | 이미지 없이 텍스트 검색으로 폴백 | |
| EX-07 | 미인증 접근 | JWT 없이 API 호출 | 401 + ErrorCode 1001/1002 반환 | |
| EX-08 | 타인의 채팅방 접근 | 다른 사용자의 room_id로 접근 | 403 + ErrorCode 4002 반환 | |

---

## 3.5 성능 테스트 (Performance Test)

응답 시간과 처리량이 서비스 요구 수준을 만족하는지 검증한다.

| TC ID | 테스트 항목 | 측정 기준 | 목표 | 결과 |
|-------|-----------|----------|------|------|
| PT-01 | 첫 토큰 응답 시간 (TTFT) | 질문 전송 → SSE 첫 토큰 수신 | (작성 필요)초 이내 | |
| PT-02 | 전체 응답 완료 시간 | 질문 전송 → 전체 답변 완료 | (작성 필요)초 이내 | |
| PT-03 | 벡터 검색 소요 시간 | 하이브리드 검색 + 리랭킹 | (작성 필요)초 이내 | |
| PT-04 | 동시 요청 처리 | N명 동시 질문 시 에러율 | 에러율 0% | |

---

# 4. RAG 파이프라인 모델링 평가 (3-Stage)

RAGAS 프레임워크를 포함한 3단계 RAG 파이프라인 평가 결과를 기술한다.

## 4.1 Stage 1: 검색(Retrieval) 평가

검색 시스템이 관련 문서를 얼마나 정확하게 찾아오는지 평가한다.

| 메트릭 | 설명 | 결과 |
|--------|------|------|
| Recall@K | 관련 문서 중 검색된 비율 (재현율) | |
| Precision@K | 검색 결과 중 관련 문서 비율 (정밀도) | |
| MRR@K | 첫 번째 관련 문서의 역순위 평균 | |
| nDCG@K | 정규화된 순위 품질 | |
| delta_nDCG@K | 리랭킹 적용 전후 nDCG 개선도 | |
| delta_MRR@K | 리랭킹 적용 전후 MRR 개선도 | |

## 4.2 Stage 2: 추천(Recommendation) 평가

최종 추천된 장소의 정확도와 다양성을 평가한다.

| 메트릭 | 설명 | 결과 |
|--------|------|------|
| Precision@N | 추천 장소 중 정답 비율 | |
| Recall@N | 정답 장소 중 추천된 비율 | |
| nDCG@N | 추천 순위 품질 | |
| ILD@N | 추천 목록 내 다양성 (0=동일, 1=다양) | |
| Category Coverage | 카테고리(관광지/음식점 등) 분포 | |
| District Diversity | 지역 분포 다양성 | |
| Entity Existence Rate | 추천 장소가 DB에 실존하는 비율 | |

## 4.3 Stage 3: 생성(Generation) 평가

LLM 답변의 품질을 RAGAS 메트릭으로 평가한다.

| 메트릭 | 설명 | 결과 |
|--------|------|------|
| Faithfulness | 답변이 검색된 문서에 근거하는 정도 (환각 방지) | |
| Answer Relevancy | 답변이 질문에 적합한 정도 | |
| Context Precision | 관련 문맥이 상위에 위치하는 정도 | |
| Context Recall | 정답에 필요한 정보가 문맥에 포함된 정도 | |

---

# 5. 종합 결과 및 개선 사항

## 5.1 테스트 통과율 요약
| 테스트 유형 | 전체 | 통과 | 실패 | 통과율 |
|------------|------|------|------|--------|
| 단위 테스트 | | | | |
| API 통합 테스트 | | | | |
| 페르소나 테스트 | 40 | | | |
| 예외 처리 테스트 | | | | |
| 성능 테스트 | | | | |

## 5.2 발견된 이슈 및 개선 사항
| # | 이슈 내용 | 심각도 | 상태 | 비고 |
|---|----------|--------|------|------|
| 1 | (추후 작성) | | | |

## 5.3 결론
(추후 작성)